# OS / editor junk
.DS_Store
Thumbs.db
.vscode/

# In case any future script gains npm dependencies
node_modules/

# Development-only reference material (not used by the site itself).
# Kept locally for provenance/rework; excluded from the repo due to size
# and because it's scraped/screenshotted third-party material.
/images/Command_Lists/
/images/ClaudeCode制作用参考資料フォルダ/
/raw_data/
/Game＿System/
